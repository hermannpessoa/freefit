:80 {
  root * /app/dist
  
  # Serve files (file_server auto-detects MIME types)
  file_server
  
  # SPA routing - rewrite if file not found
  error 404 {
    rewrite * /index.html
    file_server
  }
  
  encode gzip
}
